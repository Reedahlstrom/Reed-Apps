node_modules
# dist is committed so Cloudflare Pages serves the built app directly
# (project has no build command). Rebuild with `npm run build` then commit dist.
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite
coverage
*.tsbuildinfo
