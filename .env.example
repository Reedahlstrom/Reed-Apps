# Reed Apps — environment
# Copy to .env.local and fill in when your Supabase (Postgres) project is ready.
# The app runs fully offline (localStorage) until these are set; once present it
# syncs live across devices via Supabase Realtime.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
